# PRD Generator

AI 페르소나 기반 스타트업 아이디어 검증 및 PRD 자동 생성 도구

## 주요 기능

### 🎭 커스터마이즈 가능한 AI 페르소나
10가지 전문가 페르소나 중 3명을 선택하여 다각도로 아이디어를 검증합니다.

| 페르소나 | 역할 | 검토 영역 |
|---------|------|----------|
| 개발자 | 기술 전문가 | 기술적 실현 가능성, 아키텍처, 개발 비용 |
| 디자이너 | UX/UI 전문가 | 사용자 경험, UI 디자인, 접근성 |
| 투자자 (VC) | 벤처 캐피탈리스트 | 시장성, 수익 모델, 성장 잠재력 |
| 마케터 | 마케팅 전문가 | GTM 전략, 고객 획득, 브랜딩 |
| 법률 전문가 | 법률 고문 | 규제 준수, 개인정보보호, 법적 리스크 |
| PM | 프로덕트 매니저 | 제품 로드맵, 기능 우선순위, PMF |
| CTO | 기술 임원 | 기술 전략, 확장성, 보안 |
| CFO | 재무 임원 | 재무 모델, 번레이트, 수익성 |
| 최종 사용자 | 타겟 고객 | 사용 편의성, 문제 해결, 가격 적정성 |
| 운영 전문가 | 운영 담당자 | 운영 효율성, 프로세스, 확장 가능성 |

### 📊 3단계 검증 레벨
- **Lv.1 Idea Sketch**: 초기 아이디어 구체화 (친절한 피드백)
- **Lv.2 MVP Build**: 실무 중심 검증 (현실적인 피드백)
- **Lv.3 VC Defense**: 투자 심사 시뮬레이션 (공격적인 피드백)

### 📄 자동 문서 생성
검증 완료 후 자동으로 생성되는 문서:
- **PRD (Product Requirements Document)**: 프로젝트명, 핵심 기능, 기술 스택, 성공 지표
- **JD (Job Description)**: 초기 창업 멤버 채용 공고

## 기술 스택

- **Frontend**: Next.js 15, React 19, TypeScript
- **Styling**: Tailwind CSS
- **AI**: Google Gemini API (gemini-2.0-flash)
- **State Management**: React Query (TanStack Query)

## 시작하기

### 1. 설치

```bash
git clone https://github.com/chadolmin01/pcd.git
cd pcd
npm install
```

### 2. 환경 변수 설정

`.env.local` 파일을 생성하고 Gemini API 키를 설정합니다:

```env
GEMINI_API_KEY=your_gemini_api_key_here
```

> Gemini API 키는 [Google AI Studio](https://aistudio.google.com/)에서 발급받을 수 있습니다.

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

### 4. 포트 충돌 해결 (Windows)

개발 서버 실행 시 `EADDRINUSE` 에러가 발생하면:

```powershell
# 포트 3000 사용 중인 프로세스 확인
netstat -ano | findstr :3000

# 해당 PID로 프로세스 종료 (예: PID가 12345인 경우)
taskkill /PID 12345 /F

# 또는 모든 Node 프로세스 종료 (주의: 다른 Node 앱도 종료됨)
taskkill /IM node.exe /F
```

> ⚠️ **주의**: `taskkill /IM node.exe /F`는 모든 Node.js 프로세스를 종료합니다. 다른 Node 앱이 실행 중이라면 PID로 특정 프로세스만 종료하세요.

## 사용 방법

1. **AI 검증 모드** 선택
2. **3명의 페르소나** 선택 (개발자, 디자이너, 투자자 등)
3. **검증 난이도** 선택 (Sketch / MVP / Defense)
4. **아이디어 입력** 후 AI와 대화형 검증 진행
5. 각 페르소나의 피드백을 확인하고 **조언 반영**
6. 검증 완료 후 **PRD/JD 자동 생성**

## 프로젝트 구조

```
├── app/
│   ├── layout.tsx              # 루트 레이아웃
│   ├── page.tsx                # 메인 페이지
│   ├── globals.css             # 전역 스타일
│   └── api/idea-validator/
│       ├── analyze/route.ts    # 페르소나 분석 API
│       └── generate-artifacts/route.ts  # PRD/JD 생성 API
├── components/idea-validator/
│   ├── IdeaValidator.tsx       # 메인 컨테이너
│   ├── SelectionScreen.tsx     # 모드/페르소나/레벨 선택
│   ├── ChatInterface.tsx       # 대화 인터페이스
│   ├── ResultView.tsx          # 결과 표시
│   ├── geminiService.ts        # Gemini API 서비스
│   └── types.ts                # 타입 정의 및 페르소나 프리셋
├── src/
│   ├── context/Providers.tsx   # React Query Provider
│   └── lib/validationResultsStore.ts  # localStorage 저장소
└── tailwind.config.ts
```

## Vercel 배포 가이드

### 방법 1: GitHub 연동 (권장)

1. [Vercel](https://vercel.com)에 로그인
2. **Add New Project** 클릭
3. GitHub 저장소 `chadolmin01/pcd` 선택
4. **Environment Variables** 설정:
   - `GEMINI_API_KEY`: Gemini API 키 입력
5. **Deploy** 클릭

### 방법 2: Vercel CLI

```bash
# Vercel CLI 설치
npm i -g vercel

# 프로젝트 디렉토리에서 배포
cd prd-generator
vercel --prod

# 환경 변수 추가
vercel env add GEMINI_API_KEY production
# API 키 입력 후 Enter

# 환경 변수 적용을 위해 재배포
vercel --prod
```

### 환경 변수

| 변수명 | 필수 | 설명 |
|--------|------|------|
| `GEMINI_API_KEY` | ✅ | Google Gemini API 키 |

### 자동 배포

GitHub 저장소와 연결하면 `main` 브랜치에 푸시할 때마다 자동으로 배포됩니다.

```bash
git add .
git commit -m "Update feature"
git push origin main
# → Vercel이 자동으로 빌드 및 배포
```

## 라이브 데모

🔗 **https://prd-generator-ochre.vercel.app**

## 라이선스

MIT License
